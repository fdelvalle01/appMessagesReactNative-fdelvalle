import  styled  from '@emotion/native';
// Este componente nos permite tener mas de un componente styled de tipo body o lo que sea.

// flex: 1 => significa que el body ocupe todo el espacio que pueda de la pantalla. 
export const Body = styled.View`
    flex: 1;
`;

export const ScrollBody = styled.ScrollView`
    flex: 1;
`;